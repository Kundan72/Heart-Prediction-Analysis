# -*- coding: utf-8 -*-
"""Heart_predict.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1stFzGR24EBRdD3IgrZsAq0KjW9k-16HI

Step 1 Import and Load
"""

import numpy as np
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns
import sklearn as skl

from sklearn.model_selection import train_test_split
from sklearn.linear_model import LogisticRegression
from sklearn.ensemble import RandomForestClassifier
from xgboost import XGBClassifier
from sklearn.metrics import accuracy_score, precision_score, recall_score, f1_score

df = pd.read_csv('/content/drive/MyDrive/Datasets/heart.csv')

"""Step 2 Data Preprocessing"""

df.head(5)

df.tail(10)

df.info()

df.describe()

df.isnull()

df.count()

df['cp'].count

df["target"].value_counts()

"""value count values displayed in this below graph it's shows the difference 19 20 or 19 20 lacs"""

sns.countplot(x="target", data=df)
plt.show()

"""Step 3 Split Train and Test"""

x=df.drop("target", axis=1)
y=df["target"]
x.info()
print("\n \n \n")
y.info()
x_train, x_test, y_train, y_test = train_test_split(x, y, test_size=0.2, random_state=42)

"""Step 4 Scaling Feature"""

from sklearn.preprocessing import StandardScaler
scaler = StandardScaler()
x_train = scaler.fit_transform(x_train)
x_test = scaler.transform(x_test)

"""Step 5 Trained and Evaluate

Logistic Regression
"""

model = LogisticRegression()
model.fit(x_train, y_train)

# Make predictions on the testing set
y_pred = model.predict(x_test)

# Calculate and print metrics
accuracy = accuracy_score(y_test, y_pred)
precision = precision_score(y_test, y_pred)
recall = recall_score(y_test, y_pred)
f1 = f1_score(y_test, y_pred)

print("Accuracy:", accuracy)
print("Precision:", precision)
print("Recall:", recall)
print("F1-Score:", f1)



# Your calculated scores
scores = {
    "Accuracy": accuracy,
    "Precision": precision,
    "Recall": recall,
    "F1-Score": f1
}

# Plot
plt.figure(figsize=(5, 5))
plt.bar(scores.keys(), scores.values(), color=['skyblue','lavender','pink','lightgreen'])
plt.title("Logistic Regression.")
plt.ylabel("Score")
plt.show()

"""Random Forest"""

# Initialize and train the Random Forest model
model2 = RandomForestClassifier(random_state=42)
model2.fit(x_train, y_train)  # Ensure consistent variable names
y_pred2 = model2.predict(x_test)

# Calculate and print metrics
accuracy = accuracy_score(y_test, y_pred2)
precision = precision_score(y_test, y_pred2)
recall = recall_score(y_test, y_pred2)
f1 = f1_score(y_test, y_pred2)

print("Accuracy Score:", accuracy)
print("Precision:", precision)
print("Recall:", recall)
print("F1-Score:", f1)

# Your calculated scores
scores = {
    "Accuracy": accuracy,
    "Precision": precision,
    "Recall": recall,
    "F1-Score": f1
}

# Plot
plt.figure(figsize=(5, 5))
plt.bar(scores.keys(), scores.values(), color=['lavender','pink','lightgreen','skyblue'])
plt.title("Random Forest.")
plt.ylabel("Score")
plt.show()

"""XGBoost"""

import xgboost as xgb
# Initialize and train the XGBoost model
model5 = xgb.XGBClassifier(random_state=1)
model5.fit(x_train, y_train)  # Ensure consistent variable names
y_pred5 = model5.predict(x_test)

# Calculate and print metrics
accuracy = accuracy_score(y_test, y_pred5)
precision = precision_score(y_test, y_pred5)
recall = recall_score(y_test, y_pred5)
f1 = f1_score(y_test, y_pred5)

print("Accuracy Score:", accuracy)
print("Precision:", precision)
print("Recall:", recall)
print("F1-Score:", f1)

# Your calculated scores
scores = {
    "Accuracy": accuracy,
    "Precision": precision,
    "Recall": recall,
    "F1-Score": f1
}

# Plot
plt.figure(figsize=(5, 5))
plt.bar(scores.keys(), scores.values(), color=['lavender','pink','lightgreen','skyblue'])
plt.title("XGBoost.")
plt.ylabel("Score")
plt.show()

"""Confusion Matrix"""

from sklearn.metrics import classification_report, confusion_matrix
print("\nClassification Report:\n", classification_report(y_test, y_pred))


# Print the confusion matrix
conf_matrix = confusion_matrix(y_test, y_pred)
print("\nConfusion Matrix:\n", conf_matrix)

# Visualize the confusion matrix
sns.heatmap(conf_matrix, annot=True, fmt='d', cmap='Blues',
            xticklabels=['Negative', 'Positive'], yticklabels=['Negative', 'Positive'])
plt.xlabel('Predicted')
plt.ylabel('True')
plt.title('Confusion Matrix')
plt.show()

"""Correlation Matrix"""

corr_matrix = df.corr()
plt.figure(figsize=(15, 10))
sns.heatmap(corr_matrix, annot=True, cmap='YlGnBu', linewidths=0.5)
plt.title('Correlation Matrix')
plt.show()

import pandas as pd
import matplotlib.pyplot as plt

# Model scores
data = {
    "Model": ["Logistic Regression", "Random Forest", "XGBoost"],
    "Accuracy": [0.8524590163934426, 0.8360655737704918, 0.819672131147541],
    "Precision": [0.8709677419354839, 0.84375, 0.8620689655172413],
    "Recall": [0.84375, 0.84375, 0.78125],
    "F1 Score": [0.8571428571428571, 0.84375, 0.819672131147541]
}

# Create dataframe
df_scores = pd.DataFrame(data)

# Print table
print("Model Performance Comparison")
print(df_scores)

# Plot
df_scores.set_index("Model").plot(kind="bar", figsize=(10,6))

plt.title("Machine Learning Model Performance Comparison")
plt.ylabel("Score")
plt.ylim(0.75, 0.90)
plt.xticks(rotation=0)
plt.grid(axis='y', linestyle='--', alpha=0.7)
plt.legend(loc="lower right")

plt.show()